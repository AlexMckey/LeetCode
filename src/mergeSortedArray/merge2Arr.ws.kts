val a = intArrayOf(1,2,3,0,0,0)
val m = 3
val b = intArrayOf(2,5,6)
val n = 3
var ri = m+n-1
var ai = m-1
var bi = n-1

ri >= 0
bi < 0
ai < 0
if (ai < 0)
    while (bi >= 0)
        a[ri--] = b[bi--]
if (a[ai] > b[bi])
    a[ri--] = a[ai--]
else
    a[ri--] = b[bi--]
"ai:$ai, bi:$bi, a:${a.toList()}"
ri >= 0
bi < 0
ai < 0
if (ai < 0)
    while (bi >= 0)
        a[ri--] = b[bi--]
if (a[ai] > b[bi])
    a[ri--] = a[ai--]
else
    a[ri--] = b[bi--]
"ai:$ai, bi:$bi, a:${a.toList()}"

ri >= 0
bi < 0
ai < 0
if (ai < 0)
    while (bi >= 0)
        a[ri--] = b[bi--]
if (a[ai] > b[bi])
    a[ri--] = a[ai--]
else
    a[ri--] = b[bi--]
"ai:$ai, bi:$bi, a:${a.toList()}"

ri >= 0
bi < 0
ai < 0
if (ai < 0)
    while (bi >= 0)
        a[ri--] = b[bi--]
if (a[ai] > b[bi])
    a[ri--] = a[ai--]
else
    a[ri--] = b[bi--]
"ai:$ai, bi:$bi, a:${a.toList()}"

ri >= 0
bi < 0
ai < 0